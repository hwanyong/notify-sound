description = "Configure notification sounds and event-specific activations interactively."

[action]
type = "command"

[action.command]
command = "node ${NOTIFY_SOUND_EXTENSION_PATH:-~/.gemini/extensions/notify-sound}/scripts/config-audio.js"